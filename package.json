{
  "name": "tatamesmart",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "scripts": {
    "dev": "node scripts/with-app-env.mjs vite dev --host 0.0.0.0 --port 8080",
    "build": "node scripts/with-app-env.mjs vite build && node scripts/fix-ssr-export.mjs && npm run db:migrate",
    "db:migrate": "node scripts/migrate.mjs",
    "typecheck": "tsc --noEmit"
  }
}
